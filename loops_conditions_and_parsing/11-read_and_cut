#!/usr/bin/env bash
# Read and display username, UID, and home directory from /etc/passwd using a while loop
while IFS=':' read -r username _ uid _ _ _ home _; do
  echo "Username: $username"
  echo "UID: $uid"
  echo "Home Directory: $home"
done < /etc/passwd
